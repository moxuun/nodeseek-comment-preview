# nodeseek楼中楼预览

只保留楼中楼、原版布局、紧凑首页预览和预览内阅读跳转四个紧密相关的功能：

- 帖子页：楼中楼、原版评论布局切换，并读取同一帖子的其他分页来补齐引用关系。
- 首页/节点列表：普通左键点击帖子标题打开站内预览弹窗；弹窗里的回复同样按楼中楼展示，不是平铺摘要，也不直接跳转。
- 预览主帖（#0）提供点赞、加鸡腿、反对、收藏、引用、回复六个操作；回复楼层只提供点赞、加鸡腿、反对、引用、回复，不显示楼层收藏。收藏沿用 NodeSeek 的帖子收藏接口，因此收藏对象始终是当前帖子。所有会改变账号状态的操作都只有用户点击后才会请求 NodeSeek，引用和回复会在弹窗底部打开编辑区。
- 预览弹窗右下角提供论坛风格的圆形上下箭头按钮：弹窗内容滚动超过 300px 后显示，分别跳转预览顶部和底部；主页面不会显示这两个按钮。
- 预览采用紧凑排版：压缩主帖、作者信息、正文、回复卡片和楼中楼缩进的间距，让首屏显示更多内容；该样式只作用于预览弹窗。
- 预览会移除评论头部无效的灰色楼层按钮，把发布时间与作者信息排列在同一行，并将跨页来源链接放到评论卡片右上角。
- 预览图片支持点击放大、滚轮缩放、拖动、Esc 关闭，并提供“打开原图”入口；右下角上下按钮会避开弹窗滚动条。
- 预览框占满可视高度，并保留更宽的左右灰色边距；点击灰色区域仍可退出预览，也可以通过右上角关闭按钮或 Esc 退出。
- 预览中的“打开原楼层”新标签页会自动读取同帖分页，并复用预览页的楼中楼树构建和递归渲染，直接定位到对应楼层。
- 标题栏提供“回复帖子”，不携带楼层引用；评论卡片中的“回复”仍然是回复对应楼层。
- 预览框最右侧提供刷新、回到顶部、回到底部三个按钮；刷新沿用 V2Next 的体验：保留当前内容，在后台重新读取帖子及楼中楼，准备完成后一次性替换，并按当前正文/楼层锚点恢复位置。
- 预览首屏先渲染当前分页，其他分页并发懒加载；分页并发上限为 2，评论卡片使用浏览器懒渲染，减少首次等待和长帖渲染压力。

Ctrl/Command 点击、鼠标中键和右键菜单保持 NodeSeek 原生行为。

## 安装

1. 安装 Tampermonkey 或 Violentmonkey。
2. 打开 `nodeseek-comment-preview.user.js` 并安装。
3. 刷新 NodeSeek 页面。

脚本使用 `@grant none`，不加载远程模块，不执行远程代码。

## 使用

帖子页右下角“跳回顶端/回到底部”按钮组上方悬浮“评论布局”切换，包含两个按钮：

- `楼中楼`：识别 `@用户 #楼层`，把回复嵌套到对应楼层，并读取最多 50 页同帖评论。
- `原版`：恢复当前页面由 NodeSeek 输出的原始评论顺序和布局。

列表页普通点击帖子标题会打开预览；预览会先显示主帖 #0，再读取同帖分页并重建楼中楼。主帖底部有完整六项操作，回复楼层底部没有收藏项。点击“引用”或“回复”可在弹窗编辑并发送，点击“打开原帖回复”可回到 NodeSeek 原生编辑器；弹窗内也可以点击“新标签打开”进入原帖。

## 故障排查

如果楼中楼读取失败，可以点击 `原版`，脚本会恢复当前页面原始评论。Cloudflare、网络超时或登录状态可能导致跨页读取失败。

如果首页点击仍然跳转，请先确认旧版本脚本已卸载或更新到 `0.5.6`，然后强制刷新页面。

预览图片会使用 NodeSeek 来源嵌入，避免被图片站误判为直链；如果图片站仍拒绝嵌入，弹窗会显示失败提示。

预览会保留 NodeSeek 服务端已经渲染好的 Markdown/HTML 结构、代码高亮 class 和代码块滚动样式，并为 `pre > code` 代码块补回论坛常用的复制按钮。复制只读取当前代码块文本，不读取剪贴板；跨页懒加载出来的代码块也会自动补上按钮。

支持 NodeSeek 实际输出的 `.nsk-magic-tabs` / `.nsk-magic-tab-title` / `.nsk-magic-tab-body` 标签页点击切换，以及 `language-ansi` 代码块中的 `data-ansicode` ANSI 前景色、背景色、粗体、下划线和退格符清理；同时兼容仍保留 `:::: tabs` / `::: tab-item` 原文的页面。

0.5.1 修复分页打开原帖链接固定跳第 1 页、预览操作菜单无法用 Enter/空格键触发，以及主页面楼中楼只使用 2 个并发请求的问题。

0.5.2 修复刷新后偶尔改变阅读位置的问题：刷新期间保留旧内容，跨页评论准备完成后再替换，并通过帖子/评论锚点及懒加载高度校正保持当前视点。

0.5.3 移除跨页评论卡片中重复的“来自第 X 页 · 用户名”文字；“打开原楼层”仍然保留。帖子页和预览页现在共用同一套楼中楼树构建及递归渲染函数。

0.5.4 新标签页的跨页楼层也补齐点赞、加鸡腿、反对、引用、回复操作，并将“打开原楼层”放到楼层卡片右上角；楼层收藏仍不显示。

0.5.5 修复预览页回复编辑器只能共存一个且总是追加到弹窗底部的问题；现在每个楼层拥有独立回复编辑器，直接显示在对应楼层操作栏下方，帖子级“回复帖子”仍单独管理。

0.5.6 修复回复成功后跨页评论仍命中旧缓存的问题；预览打开、预览刷新和帖子页分页刷新都会强制读取最新同帖 HTML，回复发送成功后自动重新整理楼中楼。

0.5.7 将油猴脚本名称改为 `nodeseek楼中楼预览`。

0.5.8 修复帖子页在楼中楼模式下发送回复后重新整理时，被嵌套进回复列表的当前页楼层会丢失（例如 9 条回复只剩 4 条）的问题；预览弹窗中“打开原楼层”现在只出现在跨页评论卡片上，与帖子页行为一致。
0.5.9 修复预览弹窗与跨页评论的点赞、鸡腿、反对、收藏计数显示为 0 的问题：NodeSeek 的评论菜单由 Vue 客户端渲染，服务端 HTML 只输出空的挂载点；现在从服务端 `<script id="temp-script">` 的 SSR 状态（base64 JSON）恢复每条评论的真实计数（点赞 `upvoteCount`、鸡腿 `likeCount`、反对 `dislikeCount`），帖子级收藏数取自 `collectionCount`，并据此标记已收藏/未收藏状态。

0.5.10 帖子页的“楼中楼/原版”评论布局切换改为右下角悬浮，固定在站点“跳回顶端/回到底部”按钮组的正上方并右对齐，新标签页打开帖子时同样可见，不再占用评论区上方的版面。

0.5.11 修复帖子正文的 NodeSeek 投票面板（`nsapp://vote` 组件）在预览弹窗和跨页克隆里不显示的问题：投票面板由 Vue 客户端渲染，SSR HTML 只有 `nsapp://vote` 链接——预览解析链接后经同源接口 `/api/vote/info/{id}` 拉取选项，由脚本重建可交互面板，提交走 `/api/vote/voteforitem`；任何外来 `form`/`input`/`button` 仍一律删除。界面增加跟随 NodeSeek `body.dark-layout` 的黑夜模式，覆盖帖子页悬浮工具栏、回复编辑器与投票面板。

0.5.20 将脚本重构为按职责构建的 26 个源码模块：配置/运行时、DOM/URL、SSR/身份、HTTP/分页、评论树、官方楼层渲染、预览入口/弹窗/滚动、图片灯箱、内容增强、评论动作、投票、帖子页控制器和启动事件分别维护；身份识别不再读取 Cookie 或 Storage。构建产物由 `work/build.mjs` 生成，当前本地浏览器回归测试为 21/21。

## 安全边界

- 只读取当前 NodeSeek 帖子页面的同源 HTML。
- 不读取 Cookie、密码、令牌、剪贴板、私信或浏览历史。
- 不自动点赞、回复、感谢、签到、关注或发送通知；点赞、鸡腿、反对、收藏和发送回复都必须由用户主动点击。
- 远程评论先经过清理再显示；新标签页和预览页的楼层操作栏统一由脚本接管，实际请求仍沿用 NodeSeek 同源接口。

## 检查

```powershell
node --check .\outputs\nodeseek-comment-preview.user.js
```

## 回归测试

本地 fixture + 无头浏览器端到端测试，覆盖：楼中楼构建与跨页来源链接、原版/楼中楼切换、楼层链接跳转、帖子页回复后重排保留全部楼层（0.5.8）、点赞/收藏接口、预览弹窗结构与操作菜单、跨页来源链接范围（0.5.8）、楼层/帖子级回复编辑器、刷新保留滚动位置、弹窗发送回复后重排、标签页/ANSI/复制按钮、投票提交后的结果视图、暗色模式、列表外链接原生跳转和运行时异常收集。

```powershell
cd .\work\test
npm install
npm test
```

测试自动启动本地 fixture 服务器（随机端口）并在结束时关闭；需要本机 Chromium/Chrome/Edge，找不到时用 `CHROME_PATH` 指定浏览器可执行文件。全部通过退出码为 0，任一失败为 1。
